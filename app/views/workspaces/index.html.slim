= render partial: 'workspaces', locals: { workspaces: @workspaces }
.col-md-5.text-md-end.pt-md-0.pt-2
  = link_to 'Создать пространство', new_workspace_path, data: { turbo_frame: :modal }, class: 'btn btn-sm btn-outline-primary mb-1'

= Time.now
= link_to 'go', destroy_user_session_path, data: {turbo_method: :delete}
